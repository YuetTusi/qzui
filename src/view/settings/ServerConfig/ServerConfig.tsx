import React, { Component, ReactElement } from 'react';

/**
 * @description 服务器配置
 */
class ServerConfig extends Component {
    constructor(props: any) {
        super(props);
    }
    render(): ReactElement {
        return <div>服务器配置</div>
    }
}
export default ServerConfig;